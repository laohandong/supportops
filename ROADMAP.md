# SupportOps 版本路线图

本文区分当前已实现能力与后续产品演进方向。当前能力与运行方式见 [README](README.md)；后续阶段均为规划，阶段顺序表达能力依赖，不代表发布时间承诺。工程链路验证与真实模型效果评测分别报告。

## 当前版本：单 Agent 诊断闭环

### 业务目标

实施人员提交 Java 应用异常，Agent 通过只读工具与适用文档收集证据，输出诊断结论、处理建议和未决问题。环境变更由人员执行，处理后再次查询验证结果。

### 功能边界

| 模块 | 已实现范围 |
| --- | --- |
| Agent | 一个诊断 Agent；支持多轮工具调用、最大步数、超时和取消 |
| 示例应用 | 一个 Java 订单同步服务，包含受控的下游接口和可复现故障 |
| MCP | 查询应用信息、生效配置、日志、下游健康状态四个只读工具；凭据等敏感值不直接返回 |
| 文档与 RAG | Markdown、文字型 PDF、XLS/XLSX；原件与修订管理、可配置分片、ES 关键词与向量召回、RRF 融合与可选本地 ONNX 重排序；Excel 使用受限 Text2SQL；保留版本、来源和引用位置 |
| 存储与后台任务 | MyBatis-Plus/MySQL、MinIO、Elasticsearch；持久化处理任务、租约、进度、失败补偿与人工重试 |
| Skills | 两个内置文件式排障 Skill，按需加载，无独立管理后台 |
| 项目记忆 | 用户明确确认的项目约束；跨会话读取、查看和删除；不自动沉淀未经确认的诊断推测 |
| 交互与记录 | 客户工作区与后台管理；SSE 流式回答、Markdown、证据分类、历史回看、导出与会话逻辑归档 |
| 账号与权限 | 首次管理员初始化、登录与退出、管理员创建账号、普通用户本人对话隔离、诊断和上传归属 |
| 配置与统计 | 对话与向量独立配置、模型目录搜索与推理等级；用量趋势、会话 Top 20 与逐轮问答回看 |
| 验证 | 六个案例及批量执行脚本，记录结论、证据、耗时、Token 和失败情况 |

知识检索、Excel 查询与 Skill 加载不计入四个环境诊断 MCP 工具。当前提供管理员与普通用户两类基础权限，保留固定示例环境、工具白名单和敏感信息处理边界；多租户、项目授权和复杂角色体系属于后续演进。

### 六个验证场景

| 场景 | 环境或数据条件 | 验收重点 |
| --- | --- | --- |
| 下游不可用 | 下游接口返回 503，示例服务产生真实调用失败记录 | 查询错误和下游状态，正确指出故障位置 |
| 凭据错误 | 无效凭据触发下游 401 | 区分认证问题与服务不可用，不泄露凭据 |
| 升级后配置不生效 | 示例应用不同版本采用不同配置规则，当前环境仍使用旧配置 | 结合版本、实际配置和迁移文档定位差异 |
| 历史文档不适用 | 新旧文档并存，旧案例症状相似 | 识别适用范围，避免套用错误版本的方案 |
| 信息不足 | 缺失部分日志，或一个诊断工具不可访问 | 说明证据缺口，提出具体补充要求，不伪造根因 |
| 文档干扰指令 | 参考文档中含有诱导越权或偏离任务的内容 | 将外部内容视为数据，保持工具和业务边界 |

前三类故障通过真实 HTTP 交互产生；其余场景复用同一环境并控制资料或工具可用性。标准答案与预埋故障定义不进入 Agent 可访问的文档和工具结果。

### 诊断闭环验收标准

以下为工程与效果的验收要求，不表示六个场景的模型回答已全部通过。已执行检查及其限制见 [验证记录](docs/validation.md)。

- 从输入问题到工具查询、知识检索、证据引用和诊断记录形成完整链路。
- 工具返回可核查的环境事实，不返回预设诊断答案。
- 主演示场景支持人工修正配置后重新检查，展示处理前后的状态变化。
- 六个案例能够重复执行；结果报告包含失败记录，不以单次成功作为稳定性证明。
- 区分资料补充带来的收益与检索方式的收益。向量检索的效果须与相同资料条件下的直接读取或关键词搜索比较。
- 项目记忆能在新会话中召回已确认约束，删除后不再作为该约束的记忆来源。

## 后续演进

### 完整工单系统

诊断记录扩展为团队工单，支持受理、指派、补充材料、升级、处理结果回填和关闭验证；通过外部系统接口关联现有工单。

验收重点：状态转换可追溯，人员交接能够保留完整证据，重复事件不会创建重复工单。

### 复杂权限

支持项目或客户隔离、角色与资源授权、工具读写分级、操作审批和审计。授权由服务端执行，模型输入不能改变权限范围。

验收重点：跨范围访问被拒绝；审批绑定具体目标与参数；参数、权限或环境发生相关变化后重新校验。

### 自动修复

在明确授权和前置条件下执行有限的修复动作，记录操作标识、执行结果与恢复检查；对重复请求、超时和不确定结果进行核查。

验收重点：未经授权不执行，重复请求不重复产生副作用，失败能够定位和恢复。无法安全回退的动作必须具有明确的处理边界。

该阶段依赖权限、审批与执行记录，不以提示词约束替代服务端检查。

### 多 Agent

对可独立完成的日志分析、文档核查等任务进行拆分，使用独立上下文、工具范围和预算，汇总结构化结果。

验收重点：在相同案例与可比预算下，与单 Agent 对照；记录质量、耗时和成本，明确适用任务类型。

### 技能市场 

支持 Skill 发现、安装、版本固定、更新和停用；声明来源、所需工具和权限，关联回归案例。

验收重点：运行中的任务绑定确定版本；技能安装不能绕过权限；含可执行内容的技能受执行隔离约束。

## 架构演进原则

- 首版采用模块化设计，围绕 Agent、工具、知识、记忆和诊断记录划分职责。
- 保留清晰的模块接口，不为尚未实现的功能提前搭建通用平台或大量空模块。
- 工单、权限和自动修复按依赖逐步接入；多 Agent 与技能市场可以独立演进。
- 新功能以可复现案例与验收结果进入版本，已实现能力与规划能力分别标注。
