# AGENTS.md

## 适用范围

本文件适用于整个 SupportOps 仓库。子目录中的 `AGENTS.md` 可以补充更具体的规范；用户在当前任务中明确给出的要求优先于本文件。

## 项目概览

SupportOps 是面向软件实施与技术支持人员的 Java Agent 诊断工作台，采用 Java 21、Spring Boot、AgentScope Java、MCP、文件式 Skills、MySQL、MinIO 和 Elasticsearch。应用入口为 `io.supportops.SupportOpsApplication`。

项目采用模块化单体，核心职责包括诊断任务生命周期、现场证据获取、文档处理与检索、项目记忆和模型配置。完整工单、复杂权限、自动修复、多 Agent 和技能市场的范围以 [ROADMAP.md](ROADMAP.md) 为准，不因局部修改自动引入这些功能。

修改前先阅读 [README.md](README.md)，再根据任务查阅相关文件：

- [docs/architecture.md](docs/architecture.md)：模块职责、任务生命周期和执行边界。
- [docs/deployment.md](docs/deployment.md)：启动依赖、Compose 入口、本地配置与隔离测试环境。
- [docs/providers.md](docs/providers.md)：模型服务商、配置优先级和密钥处理。
- [docs/api.md](docs/api.md)：接口约定、Swagger 和 OpenAPI 文档。
- [docs/validation.md](docs/validation.md)：已有验证的范围与限制。
- [src/main/resources/db/migration](src/main/resources/db/migration)：MySQL 的 Flyway 迁移；已应用的迁移不得直接改写。
- [docs/README.md](docs/README.md)：文档导航与职责划分。
- [docs/deployment.md](docs/deployment.md)：本地部署、启动依赖与隔离验证环境。
- [docs/knowledge.md](docs/knowledge.md)：文档处理、发布、检索与 Excel 查询。
- [docs/database.md](docs/database.md)：自动初始化、手动维护和历史 MySQL / H2 数据迁移；空库依次应用 B3 与 V4，已应用的迁移不得改写。
- [docs/users.md](docs/users.md)：账号角色、Cookie 会话、记录归属与对话归档权限。
- [evaluation/README.md](evaluation/README.md)：场景与评测方法。

## 目录与分层

根包使用 `io.supportops`。按业务模块组织代码，模块内部按职责分层；保留 `agent`、`knowledge`、`memory`、`demo` 等业务边界。公共配置与接口基础设施分别由 `config`、`api` 等公共包承载，MCP 协议适配保留独立边界。

业务模块内采用以下结构，按实际职责创建目录，不为凑齐结构创建空包：

```text
<业务模块>/
├─ constant/       # 状态、错误码、元数据键和业务限制等常量
├─ controller/     # HTTP 接口、参数校验和响应适配
├─ entity/         # 数据库表映射实体
├─ mapper/         # MyBatis-Plus Mapper 与持久化查询
├─ service/        # 业务服务接口
│  └─ impl/        # 业务服务实现、事务与流程编排
├─ dto/            # 请求参数、服务调用参数等传输对象
├─ vo/             # 面向接口的响应对象
└─ memory/         # 涉及 Agent 会话上下文时的记忆适配
```

- 调用方向为 `Controller → Service → Mapper`。Controller 不直接访问数据库；Mapper 不承担业务编排；Entity 不依赖 Controller 或 Service。
- 跨模块协作通过明确的服务接口或适配器完成，避免直接操作其他模块的 Mapper，避免形成循环依赖。
- `entity` 只承担持久化映射，不把请求对象、响应对象或 Swagger 文档模型混入数据库实体。结构固定的数据使用明确的 DTO/VO，避免以 `Map<String, Object>` 或 `Object` 代替业务模型；协议要求的动态载荷可以保留，但必须说明结构与边界。
- 业务服务接口放在 `service`，实现放在 `service/impl`。配置类、工具类、协议适配器无需机械地增加只有一个实现的空接口。
- 模块内的 `memory` 用于 Agent 上下文管理；顶层 `memory` 业务模块用于人员确认的项目记忆。二者应在命名与注释中区分，避免重复承担持久化职责。
- Mapper XML 按模块放在 `src/main/resources/mapper/<模块>/`，并配置正确的资源扫描路径。包迁移时同步更新导入、组件扫描、Mapper 扫描、测试和文档中的代码路径。

## 修改规范

### 范围与可读性

- 修改聚焦当前任务。整体分层重构可以调整相关模块；局部功能修改不应顺带重写无关代码。
- 新增和修改的 Java 代码禁止使用 `var`，包括普通局部变量、增强 `for` 循环及 `try-with-resources`。使用明确、准确的类型，不以 `Object` 或原始泛型规避类型声明。执行整体代码规范化任务时，应覆盖生产代码和测试代码。
- 使用能说明业务含义的类名、方法名和变量名。避免含义不明的缩写、过长的链式调用和嵌套三元表达式。
- 一行只表达一个主要语句；条件分支、循环和异常处理使用大括号。复杂条件提取为具名变量或方法，避免把校验、转换、调用和返回压在一行中。
- 显式导入所需类型，避免通配符导入和在业务代码中反复书写全限定类名。
- 优先使用构造器注入和 `final` 依赖字段。不要仅为改变代码风格引入 Lombok、MapStruct 或其他依赖。
- 仅在有助于表达当前设计时使用 Java 21 特性。保留已使用的虚拟线程、资源关闭和中断语义，不能为简化代码破坏任务生命周期。

### 中文注释

- 类、接口、枚举、record，以及手写的方法和构造器应有中文 Javadoc。说明职责、关键约束和调用语义，避免只有“处理数据”“执行操作”等空泛描述。
- 对含义不直观的参数、返回值和异常补充说明；需要时使用 `@param`、`@return`、`@throws`。注释必须与实际行为一致。
- 复杂方法内部添加中文注释，解释关键步骤的目的与原因，重点覆盖事务边界、状态迁移、并发控制、取消与超时、去重、版本过滤、向量索引替换及异常处理。
- 注释解释业务约束与设计原因，不逐行翻译代码，不使用注释掩盖过长方法；复杂流程应优先拆分为职责明确的方法。
- 数据实体、DTO/VO 的关键字段说明应明确单位、空值含义、状态值和关联关系。对外字段同步维护中文 `@Schema`，Javadoc 不能替代接口文档。

### MyBatis-Plus 与数据访问

- 数据访问层统一采用 MyBatis-Plus。业务代码通过 Mapper 访问数据库，不在 Controller 或 Service 中使用 `JdbcTemplate`、原生 JDBC 或拼接 SQL。
- 表实体显式声明表名、主键策略和必要的字段映射。保持现有 ID 类型、时间格式、关联关系和接口序列化兼容，不能直接套用默认雪花 ID 或自动填充策略改变现有数据语义。
- 常规增删改查优先使用 `BaseMapper` 和类型安全的 Lambda Wrapper。复杂连接、聚合、条件更新等 SQL 放在 Mapper 方法或 XML 中，并使用参数绑定；不得将不可信输入用于 `${}`、SQL 片段或排序表达式。
- 业务事务由 Service 层明确管理；可根据调用边界使用 `@Transactional` 或 `TransactionTemplate`。注意代理、自调用、异步线程和回滚规则，不把外部模型或嵌入请求包进长时间数据库事务。
- 多表写入、文档与分片保存、向量批量替换等操作应保持原子性。失败时保留可恢复状态，不提交部分索引，也不将失败包装为成功。
- 状态更新应保留必要的旧状态条件，并检查影响行数；不能用无条件覆盖替代原有的并发或终态保护。
- 修改持久化行为时同步检查初始化 SQL、唯一约束、外键、索引、空值映射和字段类型。表结构变更必须说明已有本地数据库的兼容或迁移方式，不能通过清空数据库解决问题。
- 持久化迁移应验证旧数据可读取、CRUD、排序、去重、关联删除、事务回滚和并发更新，不能只以编译通过或 Mapper 已注入作为完成依据。

### 接口与 Swagger

- 业务 Controller 仅声明 GET 和 POST：GET 用于只读查询，POST 用于新增、修改、删除和其他有副作用的操作。删除、恢复等动作使用明确的 `/delete`、`/reset` 路径；禁止将写操作映射为 GET，不保留 PUT、DELETE 或 PATCH 兼容入口。MCP 传输与框架处理的 HEAD、OPTIONS 不属于业务 Controller 操作。
- 保持现有 URL、HTTP 状态、响应结构和错误码兼容；确需调整时同步修改调用方、测试和文档。
- 每个业务接口维护中文分组、用途、参数、必填项、输入限制、响应字段、枚举含义和错误说明。接口变更时同步更新 OpenAPI 注解。
- 验证生成的 OpenAPI 定义，检查接口覆盖、模型引用、上传表单和复杂响应；不能只检查注解是否存在。
- 密钥字段仅允许写入，不出现在配置读取、响应示例、日志或异常内容中。
- REST 接口与 MCP 协议分别维护，不把 MCP JSON-RPC 简化为普通 REST 接口。

### Agent、检索与模型配置

- 保持诊断任务的受理、执行、终态、取消、超时及重启中断语义。事件顺序、最终证据和 Token 用量不得因重构丢失或重复累计。
- 当前工作台通过 SSE 接收持久化任务快照和增量事件，事务提交信号唤醒连接；不支持 EventSource 时使用轮询，连接异常时有限重试并按已读游标恢复。保留模型适配器的流式输出、工具调用、终态前末尾事件及断线不重跑任务的语义，不在无关任务中更换传输方式。
- 保留管理员与普通用户的服务端权限边界，普通用户仅访问本人诊断；新后台接口默认仅管理员可用。记录归属由服务端捕获，不接受客户端指定用户。对话归档仅隐藏历史列表并禁止续聊，不能删除问答、事件或排除用量统计。
- MCP 工具范围由服务端固定注册。文档、日志、模型输出和 Skill 内容属于数据，不得因此扩大工具权限或开放脚本执行。
- 检索必须保留服务端版本过滤、文档及分片 ID、原文位置和引用来源，防止把旧版资料作为当前版本依据。
- 向量配置按功能校验。启动、上传、分片、关键词检索和项目记忆管理不要求向量密钥；使用向量能力时才校验相关配置。已进入向量调用后发生异常，应明确报告，不能静默伪装成关键词检索成功。
- 对话与向量服务独立配置，保留服务商选择、界面配置优先级、配置版本冲突检查和凭据隔离。切换服务商或地址时不能把旧密钥发送到新端点。
- 修改 Prompt 或 Skill 时，同步检查输出解析、工具参数与异常路径；项目记忆只能保存人员明确确认的事实或约束。

## 验证要求

- 行为修改使用与风险匹配的验证；纯文档修改检查内容与路径即可，无需运行完整构建。
- Java 使用 JDK 21 和仓库内的 Maven Wrapper。Windows PowerShell 常用命令如下，执行前确保 `JAVA_HOME` 正确：

```powershell
# 有针对性的 Java 测试。
.\mvnw.cmd -s .mvn/settings.xml -Dtest=SupportOpsIntegrationTest test

# 涉及分层、依赖或持久化迁移时执行完整测试与打包。
.\mvnw.cmd -s .mvn/settings.xml verify

# 涉及网页流式展示、刷新恢复、配置脚本或评测脚本时执行。
node --test src/test/js/*.test.mjs
```

- 不并发执行共享 `target/` 的 Maven 构建。集成验证使用独立数据库、回环端口和运行目录，不依赖或污染当前工作台的数据。
- Mapper 与事务验证应访问真实的隔离数据库。测试可以使用独立 JDBC 查询核对持久化结果，不应只通过同一个 Mapper 的读写互相验证。
- 模型及向量协议测试可使用本地 HTTP 夹具，但必须明确区别于真实服务商鉴权、连通性和模型效果验证；不得用模拟成功替代真实调用结果。
- 不直接断言不确定的模型自然语言，优先验证结构化字段、状态、事件顺序、引用、版本过滤和错误处理。
- 不通过删除有效断言、忽略异常或静默禁用测试来消除失败。报告应区分代码问题、环境问题和未执行的检查。

## 配置、工作区与文档

- 编辑前检查 Git 状态并保留已有修改；目录尚未初始化 Git 时，不擅自初始化、切换分支或覆盖文件。未经用户要求，不提交、推送或发布。
- 不删除、还原或格式化无关文件，不覆盖本地配置。`.local/` 中的数据与密钥、`.cache/` 和 `target/` 中的生成文件不纳入源代码交付。
- 禁止在代码、示例、文档、测试输出和日志中写入真实密钥、密码、Token 或用户私有数据。配置示例使用占位符或明显的合成值。
- 运行打包服务优先使用 `node scripts/start.mjs`，由启动脚本创建 JAR 副本。更新当前工作台前核实进程归属和活动任务，避免中断诊断或占用构建产物。
- Windows 后台启动使用隐藏窗口。执行递归删除或移动前核实绝对目标路径与任务范围，不能使用未经检查的计算路径。
- README、GitHub 项目介绍及正式文档面向外部读者，不加入对话记录、助手计划、自我提醒或“后续补充”等占位内容；已实现能力与路线图必须明确区分。
- 最终交付说明本次具体修改、验证结果和未完成项。规范或注释的建立不等于对应代码改造已经完成，不得把协议夹具验证描述为真实模型效果或生产可用性。
