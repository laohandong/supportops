# OrderBridge 1.0 历史手册

## 适用范围

仅适用于 OrderBridge 1.0。此版本使用 `orders.path` 指定订单同步路径，未配置时使用 `/v1/orders`。1.0 配套下游接口支持 `/v1/orders`。

## 历史排查记录

在 1.0 的一例订单同步配置错误中，实施人员将 `orders.path` 恢复为 `/v1/orders` 后，业务请求返回成功。

该历史处理方式依赖 1.0 的配置规则和配套下游版本，不能用于推断 2.0 的行为。
